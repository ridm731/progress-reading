# Auth.js
AUTH_SECRET=""           # openssl rand -base64 32 の出力
AUTH_GOOGLE_ID=""        # Google Cloud Console → OAuth 2.0 クライアントID
AUTH_GOOGLE_SECRET=""    # Google Cloud Console → OAuth 2.0 クライアントシークレット
ALLOWED_EMAIL="your-email@gmail.com"

# Neon (PostgreSQL)
DATABASE_URL=""          # Neon Console → Connection String（Direct接続、Pooledではない）

# フェーズ1: モックデータを使う。フェーズ3で削除する
USE_MOCK="true"

# AI (フェーズ4)
# GEMINI_API_KEY=""      # NEXT_PUBLIC_ は絶対につけない
